<%= form_for :tweet, url: tweets_path, method: :post do |f| %>
  <div class="field">
    <%= f.text_area :message, placeholder: "Compose new tweet..." %>
    <%= f.submit "Send Tweet", class: "btn btn-large btn-primary"%>
  </div>
<% end %>
